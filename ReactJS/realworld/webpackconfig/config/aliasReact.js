const projectRoot = require('../config/pathReact').projectRoot + "/www/js/source/reactpages";

const alias = {
    lrfcomponents: projectRoot + "/components",
    lrflib: projectRoot + "/lib"
};

module.exports = alias;